# Environment variables for the Supabase Edge Function

# Base URL of the API server that the function will call
# In production, this should be your deployed API server URL
# In development, this can be localhost
API_BASE_URL=http://localhost:3000

# Example: If your API is deployed on a different domain
# API_BASE_URL=https://your-api-domain.com